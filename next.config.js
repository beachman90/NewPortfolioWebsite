/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost', 'https://jan-inge-portfolio-lxice5dew-beachman90s-projects.vercel.app/'],
  },
}

module.exports = nextConfig